app = "anchor-backend"

[build]
  dockerfile = "apps/backend/Dockerfile"
  context = "apps/backend"

[env]
  PORT = "8080"

[[services]]
  internal_port = 8080
  protocol = "tcp"

  [[services.ports]]
    port = 80
